To use a digit character as an array subscript, we would need to scale the
underlying character codes by writing `arr[d - '0']` where `d` is a digit
character and the underlying character codes are consecutive.
